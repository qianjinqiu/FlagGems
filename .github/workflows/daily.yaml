# TODO(Qiming): Enable daily tests for benchmark and all backends

name: daily

on:
  workflow_dispatch:

  schedule:
    - cron: '0 16 * * *'

jobs:
  prepare:
    runs-on: ubuntu-latest
    run: |
      echo "CURRENT_DATE=$(date +'%Y-%m-%d-%H%M%S')" >> $GITHUB_ENV

  cpp-op:
    runs-on: cpp
    uses: ./.github/workflows/cpp-op-test.yaml

  python-op:
    needs: prepare
    concurrency:
      group: python-op-${{ needs.prepare.outputs.CURRENT_DATE }}
      cancel-in-progress: true
    runs-on: jiuding
    steps:
      - uses: actions/checkout@v6
      - shell: bash
        run: tools/gpu_check.sh
      - shell: bash
        env:
          CHANGED_FILES: '__ALL__'
        run: |
          # Use CURRENT_DATE as a fake PR ID
          bash tools/test-op.sh ${{ needs.prepare.outputs.CURRENT_DATE }}

  examples:
    needs: prepare
    concurrency:
      group: examples-test-${{ needs.prepare.outputs.CURRENT_DATE }}
      cancel-in-progress: true
    runs-on: jiuding
    steps:
      - uses: actions/checkout@v6
      - shell: bash
        run: tools/gpu_check.sh
      - shell: bash
        env:
          CHANGED_FILES: '__ALL__'
        run: |
          bash tools/test-examples.sh ${{ needs.prepare.outputs.CURRENT_DATE }}

#   coverage:
#     needs:
#       - preprocess
#       - blas-op-test
#       - examples-test
#       - quick-cpu-op-test
#       - utils-test
#       - pointwise-op-test
#       - reduction-op-test
#       - special-op-test
#     if: contains(github.event.pull_request.labels.*.name, 'tests')
#     concurrency:
#       group: coverage-test-${{ github.event.pull_request.number || github.ref }}
#       cancel-in-progress: true
#     runs-on: cpu
#     continue-on-error: true
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v6
#       - name: Get python coverage
#         shell: bash
#         run: |
#           git config --global --add safe.directory ../FlagGems
#           if [ "${{ github.event_name }}" == "pull_request" ]; then
#             PR_ID=${{ github.event.pull_request.number }}
#           elif [ "${{ github.event_name }}" == "push" ]; then
#             PR_NUMBER=$(git log -1 --pretty=format:'%s' | grep -oE '#[0-9]+' | grep -oE '[0-9]+')
#             PR_ID=${PR_NUMBER}
#           fi
#           bash tools/code_coverage/coverage.sh ${PR_ID}
#       - name: Report python coverage
#         shell: bash
#         run: |
#           git config --global --add safe.directory ../FlagGems
#           if [ "${{ github.event_name }}" == "pull_request" ]; then
#             PR_ID=${{ github.event.pull_request.number }}
#           elif [ "${{ github.event_name }}" == "push" ]; then
#             PR_NUMBER=$(git log -1 --pretty=format:'%s' | grep -oE '#[0-9]+' | grep -oE '[0-9]+')
#             PR_ID=${PR_NUMBER}
#           fi
#           bash tools/code_coverage/report_coverage.sh ${PR_ID}
